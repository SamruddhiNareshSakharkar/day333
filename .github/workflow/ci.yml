name: CI

on:
  push:
    
jobs :
  test :
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - 
      - name: Install dependencies
        run: pip install pytest
          
          
      - name: Run tests
        run: pytest
          